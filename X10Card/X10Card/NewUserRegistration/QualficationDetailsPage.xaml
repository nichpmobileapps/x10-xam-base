<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="X10Card.NewUserRegistration.QualficationDetailsPage"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" 
             ios:Page.UseSafeArea="true"  >
    <NavigationPage.TitleView  >
        <StackLayout  Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" >

            <Image Source="ic_himachallogo.png"  HeightRequest="40"  VerticalOptions="CenterAndExpand"/>
            <StackLayout  Orientation="Vertical" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" Spacing="0">
                <Label x:Name="lbl_navigation_header1" Style="{StaticResource NavigationLabelColor}" FontSize="15" FontAttributes="Bold" HorizontalTextAlignment="Center"
                    Text="Department Of Labour &amp; Employment"  VerticalTextAlignment="Center" />
                <Label x:Name="lbl_navigation_header"  Style="{StaticResource NavigationLabelColor}"  FontSize="13" FontAttributes="Bold"
                       HorizontalTextAlignment="Center"  VerticalTextAlignment="Center" />

            </StackLayout>
        </StackLayout>
    </NavigationPage.TitleView>
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="ic_home.png" Clicked="ToolbarItem_Clicked" ></ToolbarItem>
    </ContentPage.ToolbarItems>

    <Grid>
        <StackLayout Margin="9">

            <StackLayout>
                <Button x:Name="btn_header" CornerRadius="5"  Text="Education Details" HorizontalOptions="FillAndExpand"  TextColor="White" BackgroundColor="Navy" FontSize="16" FontAttributes="Bold"/>
                <Frame  CornerRadius="10"  BorderColor="#058ef2" VerticalOptions="FillAndExpand" Padding="10" >

                    <StackLayout>

                        <ListView x:Name="listview_Qualificationdetails"   HasUnevenRows="True" SeparatorColor="#126b8a"  SelectionMode="None"  >
                            <ListView.ItemTemplate >
                                <DataTemplate>
                                    <ViewCell >
                                        <Frame VerticalOptions="StartAndExpand"  HasShadow="False" Margin="5" CornerRadius="20" BackgroundColor="White" BorderColor="#0072b6" >
                                            <StackLayout Orientation="Vertical">


                                                <Label FontSize="16" Text="Qualification" TextColor="Navy"  HorizontalTextAlignment="Start" FontAttributes="Bold"  Grid.Row="0" Grid.Column="0"/>
                                                <Label FontSize="16" Text="{Binding QualNm}"  HorizontalOptions="FillAndExpand"   TextColor="Maroon" Grid.Row="0" Grid.Column="1"/>

                                                <Label FontSize="16" Text="Marks(%)" TextColor="Navy"  HorizontalTextAlignment="Start"  FontAttributes="Bold" Grid.Row="1" Grid.Column="0"/>
                                                <Label FontSize="16" Text="{Binding perMarks}"  HorizontalOptions="Start" HorizontalTextAlignment="Start" TextColor="Maroon" Grid.Row="1" Grid.Column="1"/>

                                                <Label FontSize="16" Text="Passing Year" TextColor="Navy"  HorizontalTextAlignment="Start"  FontAttributes="Bold" Grid.Row="2" Grid.Column="0"/>
                                                <Label FontSize="16" Text="{Binding year}"  HorizontalOptions="Start" HorizontalTextAlignment="Start" TextColor="Maroon" Grid.Row="2" Grid.Column="1"/>

                                                <Label FontSize="16" Text="Status" TextColor="Navy"  HorizontalTextAlignment="Start"  FontAttributes="Bold" Grid.Row="3" Grid.Column="0"/>
                                                <Label FontSize="16" Text="{Binding StatusDesc}"  HorizontalOptions="Start" HorizontalTextAlignment="Start"  TextColor="Maroon" Grid.Row="3" Grid.Column="1"/>

                                                <StackLayout Orientation="Horizontal">
                                                    <ImageButton IsVisible="{Binding imgeditdeletevisibility}"  Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"  x:Name="img_edit" CommandParameter="{Binding QualCd}" Clicked="img_edit_Clicked"  HorizontalOptions="CenterAndExpand" Source="ic_edit" BackgroundColor="Transparent" HeightRequest="35" />
                                                    <ImageButton  IsVisible="{Binding imgeditdeletevisibility}"  Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"  x:Name="img_delete" CommandParameter="{Binding QualCd}" Clicked="img_delete_Clicked"  HorizontalOptions="CenterAndExpand" Source="ic_delete" BackgroundColor="Transparent" HeightRequest="35" />
                                                    <ImageButton  IsVisible="{Binding pdfimgvisibility}" Grid.Row="4" Grid.Column="2" Grid.ColumnSpan="2"  x:Name="img_pdf" CommandParameter="{Binding QualCd}" Clicked="img_pdf_Clicked"  HorizontalOptions="CenterAndExpand" Source="ic_viewpdf.png" BackgroundColor="Transparent" HeightRequest="35" />
                                                </StackLayout>
                                                <!--</Grid>-->
                                            </StackLayout>
                                        </Frame>
                                    </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <Button  CornerRadius="10" x:Name="btn_AddQualification" Text="Add Qualification" HorizontalOptions="CenterAndExpand" Padding="5"  TextColor="White" BackgroundColor="Navy" FontSize="16" FontAttributes="Bold" Clicked="btn_AddQualification_Clicked"/>

                    </StackLayout>


                </Frame>

                <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                    <Button IsVisible="false"  x:Name="btn_cancel1" FontSize="18" Text="Cancel" Margin="0,10,0,10" BackgroundColor="#0072b6" TextColor="White" CornerRadius="15"  HorizontalOptions="FillAndExpand" Clicked="btn_cancel1_Clicked" Padding="10"/>
                    <Button  IsVisible="false"  x:Name="btn_next" FontSize="20" Text="Next" Margin="0,10,0,10" BackgroundColor="#058ef2" TextColor="White" CornerRadius="15"  HorizontalOptions="FillAndExpand" Clicked="btn_next_Clicked" Padding="10" FontAttributes="Bold"/>
                    <Button  IsVisible="false"  x:Name="btn_submit1" FontSize="20" Text="Save &amp; Next" Margin="0,10,0,10" BackgroundColor="#058ef2" TextColor="White" CornerRadius="15"  HorizontalOptions="FillAndExpand" Clicked="btn_submit1_Clicked" Padding="10" FontAttributes="Bold"/>

                    <!--<Button  x:Name="btn_submit1" FontSize="20" Text="Save" Margin="0,10,0,10" BackgroundColor="#058ef2" TextColor="White" CornerRadius="15"  HorizontalOptions="FillAndExpand" Clicked="btn_submit1_Clicked" Padding="10" FontAttributes="Bold"/>-->
                </StackLayout>
            </StackLayout>



        </StackLayout>



        <ContentView x:Name="popupaddqualification" BackgroundColor="#C0808080" Padding="10" IsVisible="false" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All" >
            <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="Center" BackgroundColor="white" Padding="10">
                <Label x:Name="lbl_popupvacancy" BackgroundColor="#0072b6" TextColor="White" Text="Add Qualification" Padding="6" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="16" FontAttributes="Bold"  />
                <Label Text="*Fields marked are mandatory" HorizontalOptions="Center" HorizontalTextAlignment="Center" TextColor="Navy" FontSize="14" />

                <ScrollView>
                    <StackLayout  Orientation="Vertical" >


                        <Label Text="SELECT QUALIFICATION *" TextColor="Navy" FontSize="16"  FontAttributes="Bold" />
                        <Editor Placeholder="Tap To Select Qualification" x:Name="editor_QualificationName" PlaceholderColor="Black" HeightRequest="50" Focused="editor_QualificationName_Focused" AutoSize="TextChanges" FontSize="16" />

                        <!--<Picker TextColor="Black" TitleColor="Black" Title="SELECT QUALIFICATION " x:Name="picker_QUALIFICATION" SelectedIndexChanged="picker_QUALIFICATION_SelectedIndexChanged"/>-->

                        <Label Text="SELECT BOARD *" TextColor="Navy" FontSize="16"  FontAttributes="Bold" />
                        <Picker TextColor="Black" TitleColor="Black" Title="SELECT BOARD " x:Name="picker_BOARD" SelectedIndexChanged="picker_BOARD_SelectedIndexChanged"/>

                        <Label Text="ENTER TOTAL MARKS" TextColor="Navy" FontSize="16" FontAttributes="Bold" />
                        <Entry x:Name="entry_TOTALMARKS" Placeholder="ENTER TOTAL MARKS" MaxLength="7" Keyboard="Numeric" TextColor="Black"  FontSize="16" ></Entry>

                        <Label Text="ENTER MARKS OBTAINED" TextColor="Navy" FontSize="16"  FontAttributes="Bold" />
                        <Entry x:Name="entry_MARKSOBTAINED"   Placeholder="ENTER MARKS OBTAINED" MaxLength="7" Keyboard="Numeric"  TextColor="Black"  FontSize="16" ></Entry>

                        <Button x:Name="btn_calclulatepercent" Text="Calculate Percentage" HorizontalOptions="CenterAndExpand" FontAttributes="Bold" Clicked="btn_calclulatepercent_Clicked" FontSize="16"/>
                        <StackLayout x:Name="stack_percentage" IsVisible="false">
                            <Label Text="PERCENTAGE *" TextColor="Navy" FontSize="16"  FontAttributes="Bold" />
                            <Entry x:Name="entry_Percentage" IsReadOnly="true"  Placeholder="ENTER PERCENTAGE" MaxLength="5" Keyboard="Numeric"  TextColor="Black"  FontSize="16" ></Entry>
                        </StackLayout>

                        <Label Text="ENTER YEAR OF PASSING *" TextColor="Navy" FontSize="16"  FontAttributes="Bold" />
                        <Entry x:Name="entry_YEAROFPASSING"   Placeholder="ENTER YEAR OF PASSING" MaxLength="4" Keyboard="Numeric"  TextColor="Black"  FontSize="16" ></Entry>


                        <Label Text="SELECT REGISTRATION DATE *" TextColor="Navy" FontSize="16"  FontAttributes="Bold" />
                        <StackLayout HorizontalOptions="FillAndExpand" >
                            <Entry TextColor="Black"  HorizontalOptions="FillAndExpand"  FontSize="16"  Placeholder="SELECT REGISTRATION DATE" x:Name="Entry_RegistrationDate" Focused="Entry_RegistrationDate_Focused" />
                            <DatePicker x:Name="datepicker_RegistrationDate" IsVisible="false"  Format="dd/MM/yyyy" DateSelected="datepicker_RegistrationDate_DateSelected"  HorizontalOptions="FillAndExpand"/>
                        </StackLayout>

                        <Label Text="Qualification Document Details" BackgroundColor="Navy" TextColor="White" Padding="5" FontSize="16"  FontAttributes="Bold" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center"/>
                        <Frame BorderColor="Navy" CornerRadius="20">
                            <StackLayout>


                                <Label Text="SELECT ISSUE DATE *" TextColor="Navy" FontSize="16"  FontAttributes="Bold" />
                                <StackLayout HorizontalOptions="FillAndExpand" >
                                    <Entry TextColor="Black"  HorizontalOptions="FillAndExpand"  FontSize="16"  Placeholder="SELECT ISSUE DATE" x:Name="Entry_issuedate" Focused="Entry_issuedate_Focused" />
                                    <DatePicker x:Name="datepicker_issuedate" IsVisible="false"  Format="dd/MM/yyyy" DateSelected="datepicker_issuedate_DateSelected"  HorizontalOptions="FillAndExpand"/>
                                </StackLayout>

                                <Label Text="ENTER CERTIFICATE NO *" TextColor="Navy" FontSize="16"  FontAttributes="Bold" />
                                <Entry x:Name="entry_certificateno"   Placeholder="ENTER CERTIFICATE NO" TextColor="Black"  FontSize="16" ></Entry>


                                <StackLayout Orientation="Horizontal" >
                                    <Label x:Name="lbl_doc1" Text="DOCUMENT" TextColor="Navy" FontSize="16" HorizontalOptions="FillAndExpand"  VerticalTextAlignment="Center" FontAttributes="Bold" Padding="5"></Label>
                                    <Label x:Name="lbl_pdf1" IsVisible="false" Text="PDF Uploaded Successfully"  VerticalTextAlignment="Center" ></Label>
                                    <Button x:Name="btn_uploaddoc1" Text="Choose File..." Clicked="btn_uploaddoc1_Clicked"></Button>
                                    <ImageButton x:Name="img_pdfedit" IsVisible="false"  Clicked="img_pdfedit_Clicked"  HorizontalOptions="CenterAndExpand" Source="ic_viewpdf.png" BackgroundColor="Transparent" HeightRequest="35" />

                                </StackLayout>


                            </StackLayout>
                        </Frame>
                    </StackLayout>
                </ScrollView>

                <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                    <Button  x:Name="btn_cancel" FontSize="18" Text="Cancel" Margin="0,10,0,10" BackgroundColor="#0072b6" TextColor="White" CornerRadius="15"  HorizontalOptions="FillAndExpand" Clicked="btn_cancel_Clicked" Padding="10"/>
                    <Button  x:Name="btn_submit" FontSize="20" Text="Save" Margin="0,10,0,10" BackgroundColor="#058ef2" TextColor="White" CornerRadius="15"  HorizontalOptions="FillAndExpand" Clicked="btn_submit_Clicked" Padding="10" FontAttributes="Bold"/>
                </StackLayout>

            </StackLayout>
        </ContentView>


        <ContentView x:Name="popupQualificationname" BackgroundColor="#C0808080" Padding="20" IsVisible="false" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All">
            <StackLayout HorizontalOptions="Center" VerticalOptions="Center" BackgroundColor="white">
                <Label x:Name="lbl_popupQualificationname" Text="Tap To Select Qualification Name" Padding="6" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="16" FontAttributes="Bold" TextColor="Navy" />
                <Frame  Padding="0" Margin="3" CornerRadius="20" BorderColor="Navy">
                    <SearchBar Placeholder="Type Qualification Name" x:Name="SearchBar_Qualificationname" IsSpellCheckEnabled="False" PlaceholderColor="#736f6f" BackgroundColor="#e0e0e0" TextColor="Black"  CancelButtonColor="Red"  Keyboard="Plain" TextChanged="SearchBar_Qualificationname_TextChanged"/>
                </Frame>

                <ListView x:Name="listview_Qualificationname" HasUnevenRows="True" SeparatorColor="Navy" ItemTapped="listview_Qualificationname_ItemTapped" BackgroundColor="White">
                    <ListView.ItemTemplate >
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Orientation="Vertical" Grid.Row="0">
                                    <Label Text="{Binding QualificationName}" HorizontalOptions="FillAndExpand" Padding="10" HorizontalTextAlignment="Start" TextColor="Black"/>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Button BackgroundColor="Navy" TextColor="White" FontAttributes="Bold"  x:Name="popupQualificationnameCancel" Clicked="popupQualificationnameCancel_Clicked" Text="Cancel" Margin="5" HorizontalOptions="FillAndExpand"/>
            </StackLayout>
        </ContentView>

        <ContentView x:Name="Loading_activity" BackgroundColor="#C0808080" Padding="20, 20" IsVisible="false" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All">
            <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                <Frame BackgroundColor="White" HorizontalOptions="FillAndExpand">
                    <StackLayout Orientation="Horizontal" Margin="6" HorizontalOptions="FillAndExpand">
                        <ActivityIndicator IsVisible="true" IsRunning="true" Margin="5"/>
                        <Label x:Name="lbl_PleaseWait" Text="Please Wait ..." TextColor="Black" FontAttributes="Bold" HorizontalOptions="FillAndExpand" WidthRequest="600" VerticalTextAlignment="Center"/>
                    </StackLayout>
                </Frame>
            </StackLayout>
        </ContentView>
    </Grid>

</ContentPage>